# Java Swing Factoring Quiz

An old project of mine from high school, back when Java Swing was more popular. 

Running the program yields an application with a simple UI that tests users on factoring quadratic equations. These equations are randomly generated and responses are entered using the built-in keyboard interface.

## TO FIX
- I originally wrote the application code in one file, which is bad modularity design.
- Many of the functions and code logic can be simplified.